# Showcase

The project-specific homepage is intentionally owned by `belief-lab`. `scripts/build-pages.sh` copies it into `dist/` and then runs the exact pinned `github-pages-template` generator in `--augment` mode. The template remains authoritative for the shared `/stats/`, `/evidence/`, `/preferences/`, localization, and presentation foundation while this repository owns the domain demo.
