[package]
name = "belief-store"
version.workspace = true
edition.workspace = true

[dependencies]
belief-core = { path = "../belief-core" }
belief-policy = { path = "../belief-policy" }
evidence-interchange = { path = "../evidence-interchange" }
inference-core = { path = "../inference-core" }

[dev-dependencies]
inference-baseline = { path = "../inference-baseline" }
serde_json = "1"
